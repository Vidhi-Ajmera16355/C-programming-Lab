#include <stdio.h>

int main()
{
    int y;
    printf("Enter any year: ");
    scanf("%d", &y);
    (y%4==0 && y%100!=0) ? printf("LEAP YEAR"):(y%400 ==0 )?printf("LEAP YEAR") : printf("Non-Leap YEAR");
    return 0;
}
